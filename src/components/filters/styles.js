import styled from 'styled-components';

const StyledFiltersContainer = styled.form`
	width: 100%;
	display: flex;
	justify-content: space-between;
	align-items: center;
`;

const StyledCheckboxContainer = styled.div`
	display: flex;
`;

export { StyledFiltersContainer, StyledCheckboxContainer };
